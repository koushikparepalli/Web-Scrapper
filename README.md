# Web Scrapper
Web scraping is a technique using which the webpages from the internet are fetched and parsed to understand and extract specific information.

The application takes search string from user, searches the query in Mongo db, if the data is not available in database, it would fetch results from Flipkart and display results to user.

